{
    "version": "0.2.0",
    "configurations": [
    
        {
            "type": "emulicious-debugger",
            "request": "launch",
            "emuliciousPath": "${workspaceFolder}/../../Resources/Emulicious",
            "name": "Launch in Emulicious",
            "program": "${workspaceFolder}/dist/Debug/game.gb",
            "port": 58870,
            "stopOnEntry": false,
            "preLaunchTask": "${defaultBuildTask}"
        }
    ]
}